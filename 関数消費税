def calculate_total_price(product_price, tax_rate=0.1):
    total_price = product_price * (1 + tax_rate)
    return total_price

# 関数を呼び出して計算結果を受け取る
result = calculate_total_price(1000, 0.1)

# 結果を表示
print(result)
